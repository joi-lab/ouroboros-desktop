{
  "name": "ouroboros-web",
  "version": "5.22.0-rc.1",
  "private": true,
  "type": "module",
  "description": "Ouroboros browser UI package boundary",
  "scripts": {
    "lint": "node --check app.js && node --check sw.js && node --check modules/pwa.js"
  }
}
