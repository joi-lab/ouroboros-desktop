openai>=1.66.0
requests
dulwich
starlette
uvicorn[standard]
websockets
huggingface_hub
# optional: browser automation tools (for browse_page/browser_action)
playwright
playwright-stealth
# optional: local model server (installed separately by download_python_standalone scripts)
# manual install: pip install llama-cpp-python[server]
# macOS Metal: pip install llama-cpp-python[server] --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/metal
